# 有研東京検査所 精度管理システム 関数フロー図

## 1. システム全体フロー

```mermaid
graph TD
    A[ユーザーアクセス] --> B[メインページ]
    B --> C{ページ選択}
    
    C -->|pg01| D[管理試料測定結果登録・確認]
    C -->|pg02| E[測定状況登録]
    C -->|pg03| F[精度管理試料測定結果・対応]
    C -->|pg04| G[管理者関係・精度管理実施状況確認]
    C -->|pg05| H[精度管理図出力]
    C -->|pg99| I[マスターテーブル管理]
    
    D --> J[データベース操作]
    E --> J
    F --> J
    G --> J
    H --> J
    I --> J
    
    J --> K[結果表示]
    H --> L[Excelファイル出力]
```

## 2. データベース接続フロー

```mermaid
graph TD
    A[アプリケーション開始] --> B[Config.setup_logging]
    B --> C[ログ設定初期化]
    C --> D[データベース接続]
    D --> E[SQLite接続確立]
    E --> F[セッション状態管理]
    F --> G[ページ処理開始]
```

## 3. WESTGARDルール判定フロー

```mermaid
graph TD
    A[測定データ入力] --> B[データ前処理]
    B --> C[統計値計算]
    C --> D[平均値・標準偏差計算]
    D --> E[SD換算値計算]
    E --> F[WESTGARDルール判定]
    
    F --> G{1-2s判定}
    F --> H{1-3s判定}
    F --> I{2-2s判定}
    F --> J{R-4s判定}
    F --> K{2of3-2s判定}
    F --> L{4-1s判定}
    
    G --> M[判定結果記録]
    H --> M
    I --> M
    J --> M
    K --> M
    L --> M
    
    M --> N[結果表示]
    M --> O[データベース保存]
```

## 4. 精度管理図出力フロー (pg05_statical.py)

```mermaid
graph TD
    A[ページアクセス] --> B[年月入力]
    B --> C{ボタン選択}
    
    C -->|データ表示| D[データベース検索]
    C -->|内部精度管理台帳出力| E[Excelファイル作成]
    
    D --> F[期間データ取得]
    F --> G[データ表示]
    G --> H[グラフ表示]
    
    E --> I[create_excel_report関数]
    I --> J[ピボットテーブル作成]
    J --> K[Excelファイル生成]
    K --> L[グラフ作成]
    L --> M[ファイル保存]
    M --> N[成功メッセージ表示]
```

## 5. データ処理フロー

```mermaid
graph TD
    A[ファイルアップロード] --> B[ファイル形式チェック]
    B --> C{形式判定}
    
    C -->|Excel| D[Excelファイル処理]
    C -->|CSV| E[CSVファイル処理]
    C -->|その他| F[エラー表示]
    
    D --> G[データ読み込み]
    E --> G
    G --> H[データ検証]
    H --> I[データ変換]
    I --> J[データベース保存]
    J --> K[処理完了]
```

## 6. ログ管理フロー

```mermaid
graph TD
    A[ログイベント発生] --> B[ログレベル判定]
    B --> C{レベル判定}
    
    C -->|DEBUG| D[デバッグログ]
    C -->|INFO| E[情報ログ]
    C -->|WARNING| F[警告ログ]
    C -->|ERROR| G[エラーログ]
    
    D --> H[ログファイル出力]
    E --> H
    F --> H
    G --> H
    
    H --> I[ローテーション判定]
    I --> J{ファイルサイズチェック}
    J -->|超過| K[ローテーション実行]
    J -->|未満| L[通常出力]
    K --> L
```

## 7. エラーハンドリングフロー

```mermaid
graph TD
    A[処理開始] --> B[try文開始]
    B --> C[メイン処理]
    C --> D{エラー発生?}
    
    D -->|Yes| E[例外キャッチ]
    D -->|No| F[正常終了]
    
    E --> G[エラーログ記録]
    G --> H[ユーザー向けエラーメッセージ]
    H --> I[エラー画面表示]
    
    F --> J[処理完了]
```

## 8. セッション管理フロー

```mermaid
graph TD
    A[ページアクセス] --> B[セッション状態チェック]
    B --> C{セッション存在?}
    
    C -->|Yes| D[既存セッション使用]
    C -->|No| E[新規セッション作成]
    
    D --> F[データベース接続確認]
    E --> F
    
    F --> G{接続正常?}
    G -->|Yes| H[ページ処理継続]
    G -->|No| I[接続再試行]
    I --> F
    
    H --> J[処理完了]
```

## 9. ファイル出力フロー

```mermaid
graph TD
    A[出力要求] --> B[データ収集]
    B --> C[データ整形]
    C --> D[ファイル形式選択]
    
    D --> E[Excel出力]
    D --> F[PDF出力]
    D --> G[CSV出力]
    
    E --> H[OpenPyXL処理]
    F --> I[PDF生成処理]
    G --> J[CSV生成処理]
    
    H --> K[ファイル保存]
    I --> K
    J --> K
    
    K --> L[ダウンロード提供]
    L --> M[処理完了]
```

## 10. データベース最適化フロー

```mermaid
graph TD
    A[最適化実行] --> B[テーブル分析]
    B --> C[インデックス確認]
    C --> D[統計情報更新]
    D --> E[不要データ削除]
    E --> F[データベース圧縮]
    F --> G[最適化完了]
``` 